# Emscripten / upstream build facts (verified against jpd002/Play- master, 2026-07-09)
- emsdk version: 4.0.1 (upstream .github/workflows/build-js.yaml, setup-emsdk@v16)
- Configure:  emcmake cmake --preset wasm-ninja
- Build:      cmake --build --preset wasm-ninja-release
- Artifacts:  build_cmake/build/wasm-ninja/Source/ui_js/Release/{Play.js,Play.wasm}
  (NOTE: master plan §2 guessed build/Source/ui_js/ — the real path uses CMake presets and build_cmake/build/wasm-ninja/. Corrected here.)
- Frontend assembly: copy Play.js->js/play_browser/src, Play.{js,wasm}->js/play_browser/public, then npm run build (CRA).
- Submodules (recursive): deps/{Dependencies,Framework,CodeGen,Nuanceur,libchdr,AltKit}
